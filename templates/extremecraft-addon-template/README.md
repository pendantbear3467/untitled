# ExtremeCraft Addon Template

1. Implement `ExtremeCraftModule`.
2. Register addon types with `ExtremeCraftApiProvider`.
3. Declare service implementation under `META-INF/services`.
4. Package and ship as an independent mod jar.
