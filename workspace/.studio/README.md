# .studio

Status: local tooling state

Useful state:

- `run_configurations.json` can be a helpful shared workspace configuration file

Not source of truth:

- `autosave/`
- `logs/`
- `recovery/`

Those folders are local operational state and should not be treated as canonical
gameplay or content source.
