# classes

Status: `LIVE_RUNTIME` shared owner

This folder is the canonical datapack source for live class definitions.

Read by:

- `progression/classsystem/data/ClassDefinitionLoader`
- `classsystem/ClassRegistry` as a compatibility bridge

Edit here to change class ids, passives, and class ability lists used by runtime.
