# modules

Status: `METADATA / VALIDATION MIRROR`

This folder is not the live owner for installable modular gear.

Used by:

- `platform/data/loader/ModuleDataLoader`

Live modular gear uses:

- `armor_modules/`
- `tool_modules/`
- `modules/**` Java runtime services

The older `item/module/**` Java path is a legacy generic module system and should
not be the first edit target for current modular gear gameplay.
