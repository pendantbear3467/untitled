# extremecraft_quests

Status: `LIVE_RUNTIME`

`quest/QuestManager` loads this folder for live quest content used by progression
tracking and `GuildQuestRewardService`.

Edit here when you want quests to appear in gameplay.

Do not use `quests/` for live quest content; that folder is metadata-only.
