# machine/menu

Forge menu containers for machine and related runtime UIs.

Role:

- expose synchronized inventory/state menus for machine interactions
- keep menu-layer transport separate from machine processing logic

Guidance:

- avoid embedding machine business logic in menu classes
- keep slot/layout behavior consistent with block entity inventories
