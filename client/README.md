# client

Reserved top-level domain for future client-focused modularization.

Current status:

- active runtime client code remains under `../src/main/java/com/extremecraft/client/**`
- this folder currently acts as a structural placeholder

Guidance:

- do not assume this folder is the canonical client source location
- place active runtime changes in the `src/` tree unless a migration plan explicitly moves modules here
